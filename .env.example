# --- Binding (choose one style) ---
# Full socket address (highest priority)
# SOCKET_ADDR=0.0.0.0:8080
# SOCKET_ADDR=[::]:8080

# Or split host + port (used if SOCKET_ADDR/BIND_ADDR/ADDR not set)
HOST=0.0.0.0
PORT=8080

# --- Queue capacity ---
PROOF_QUEUE_CAP=64

# --- Logging (optional) ---
RUST_LOG=info,axum=info,tower_http=info